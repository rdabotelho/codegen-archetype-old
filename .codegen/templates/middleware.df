template {
	sourceFile: 'middleware.ts'
	targetFile: 'src/middleware/custom/${domain.name}Middleware.ts'
	scope: 'entity'
	block(2, 2) {
		replace('DomainName', domain.name)
	}
	block(9, 23) {
		replace('DomainName', domain.name)
		replace('domainName', domain.name.lowerCase)
	}
}
