template {
	sourceFile: 'controller.test.ts'
	targetFile: 'tests/unit/controller/${domain.name.lowerCase}.controller.test.ts'
	consider: 'entity'
	block(1, 3) {
		replace('DomainName', domain.name)
	}
	block(5, 16) {
		replace('DomainName', domain.name)
		replace('domainName', domain.name.lowerCase)
	}
	block(18, 84) {
		replace('DomainName', domain.name)
		replace('domainName', domain.name.lowerCase)
	}
}
