template {
	sourceFile: 'service.ts'
	targetFile: 'src/service/${domain.name}Service.ts'
	scope: 'entity'
	block(1, 2) {
		replace('DomainName', domain.name)
	}
	block(6, 31) {
		replace('DomainName', domain.name)
		replace('domainName', domain.name.lowerCase)
	}
}
