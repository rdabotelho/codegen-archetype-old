template {
	sourceFile: 'controller.ts'
	targetFile: 'src/controller/${domain.name}Controller.ts'
	consider: 'entity'
	block(2, 5) {
		replace('DomainName', domain.name)
	}
	block(8, 65) {
		replace('DomainName', domain.name)
		replace('domainName', domain.name.lowerCase)
	}
}
